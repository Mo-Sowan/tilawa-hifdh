/// What a celebration is marking.
///
/// Kept apart from the overlay that shows it so a caller can name the kind
/// of moment without depending on the widget that draws it.
enum CelebrationType { planCreated, revisionComplete, encouragement, streak }
